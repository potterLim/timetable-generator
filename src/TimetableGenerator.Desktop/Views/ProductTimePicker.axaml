<UserControl
    x:Class="TimetableGenerator.Desktop.Views.ProductTimePicker"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    MinWidth="280"
    MinHeight="44"
    Focusable="False">
  <UserControl.Styles>
    <Style Selector="ComboBox.time-segment">
      <Setter Property="MinHeight" Value="42" />
      <Setter Property="Padding" Value="12,0" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="CornerRadius" Value="0" />
    </Style>
  </UserControl.Styles>

  <Border
      Background="{DynamicResource ControlSurfaceBrush}"
      BorderBrush="{DynamicResource ControlBorderBrush}"
      BorderThickness="1"
      CornerRadius="8">
    <Grid ColumnDefinitions="6*,1,5*,1,5*">
      <ComboBox
          x:Name="MeridiemInput"
          Grid.Column="0"
          Classes="time-segment"
          TabIndex="0" />
      <Border
          Grid.Column="1"
          Margin="0,8"
          Background="{DynamicResource ControlBorderBrush}" />
      <ComboBox
          x:Name="HourInput"
          Grid.Column="2"
          Classes="time-segment"
          TabIndex="1" />
      <Border
          Grid.Column="3"
          Margin="0,8"
          Background="{DynamicResource ControlBorderBrush}" />
      <ComboBox
          x:Name="MinuteInput"
          Grid.Column="4"
          Classes="time-segment"
          TabIndex="2" />
    </Grid>
  </Border>
</UserControl>
