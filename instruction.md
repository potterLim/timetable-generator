# Timetable Generator 사용 설명서

Timetable Generator는 여러 분반과 선택 대안 중 시간이 겹치지 않는 수강 조합을 최대 10,000개까지 찾아 비교할 수 있게 해주는 Windows 앱입니다. 생성 상한에 도달하면 현재까지 만든 시간표를 표시하고 하단 상태 영역에서 입력 조건을 좁히도록 안내합니다.

## 1. 시작하기

1. `TimetableGenerator.exe`를 실행합니다.
2. 환영 화면에서 **CSV 파일 선택**을 누르거나 CSV 파일을 점선 영역에 끌어 놓습니다.
3. 파일 검증과 시간표 생성이 끝나면 첫 번째 조합이 표시됩니다.

`input` 또는 `output` 폴더를 미리 만들 필요가 없습니다. 파일 선택을 취소하면 현재 화면을 그대로 유지합니다. CSV 오류가 발생하면 오류 화면의 **다른 CSV 선택** 또는 상단의 **CSV 불러오기**로 다시 시도할 수 있으며, 이미 열어 둔 시간표가 있으면 **이전 시간표로 돌아가기**를 선택할 수도 있습니다.

작업이 오래 걸릴 때는 취소 버튼이나 `Esc`를 누를 수 있습니다.

## 2. CSV 준비하기

### 파일 조건

- 파일 확장자는 `.csv`여야 합니다.
- 파일 인코딩은 UTF-8이어야 합니다.
- 첫 행은 아래 4열 또는 5열 헤더 중 하나와 정확히 일치해야 합니다.
- 모든 데이터 행은 선택한 헤더와 같은 열 수를 가져야 합니다.

```text
CourseId,Section,Name,TimeSlots
CourseId,Section,Name,TimeSlots,Classroom
```

### 열 설명

| 열 | 입력 규칙 |
| --- | --- |
| `CourseId` | 양의 정수입니다. 같은 `CourseId`를 가진 행 중 정확히 하나가 각 시간표에 선택됩니다. 여러 분반이나 서로 대체 가능한 과목을 같은 값으로 묶습니다. |
| `Section` | 비어 있지 않은 분반 코드입니다. |
| `Name` | 비어 있지 않은 과목명입니다. |
| `TimeSlots` | `월요일1교시`처럼 한국어 요일과 교시를 붙여 씁니다. 여러 시간은 `/`로 구분하며 1~10교시만 사용할 수 있습니다. |
| `Classroom` | 선택적인 마지막 열입니다. 값은 비워 둘 수 있습니다. 입력할 때는 마지막 공백을 기준으로 건물명과 호실을 나눈 `<건물명> <호실>` 형식이어야 합니다. 예: `공학관 301`. 공백 없는 단일 값은 사용할 수 없습니다. |

사용 가능한 요일은 `월요일`, `화요일`, `수요일`, `목요일`, `금요일`, `토요일`, `일요일`입니다.

올바른 `TimeSlots`:

```text
월요일1교시
월요일1교시/수요일1교시
토요일10교시
```

잘못된 `TimeSlots`:

```text
월요일 1교시
월1교시
월요일0교시
월요일11교시
월요일1교시//수요일1교시
```

쉼표나 줄바꿈이 들어간 과목명은 일반 CSV 규칙에 따라 큰따옴표로 감쌉니다.

### 8개 조합 예제

[기본 예제 파일](data/example_course_schedule.csv)은 세 개의 `CourseId`마다 두 분반을 제공합니다. 시간 충돌이 없으므로 `2 × 2 × 2 = 8`개 조합이 생성됩니다.

```csv
CourseId,Section,Name,TimeSlots,Classroom
1,01,알고리즘,월요일1교시/수요일1교시,공학관 301
1,02,알고리즘,화요일1교시/목요일1교시,공학관 302
2,01,자료구조,월요일3교시/수요일3교시,미래관 204
2,02,자료구조,화요일3교시/목요일3교시,미래관 205
3,01,데이터베이스,금요일2교시,과학관 401
3,02,데이터베이스,금요일4교시,과학관 402
```

서로 다른 `CourseId`의 선택 결과가 같은 요일과 교시에 겹치면 해당 조합만 제외됩니다.

## 3. 시간표 비교하기

- 왼쪽 일정 목록에서 원하는 번호를 선택합니다.
- 화면 위의 이전·다음 버튼으로 한 조합씩 이동합니다.
- `Alt+Left`와 `Alt+Right`로도 이전·다음 시간표를 볼 수 있습니다.
- 새 CSV를 열려면 상단의 **CSV 불러오기** 버튼이나 `Ctrl+O`를 사용합니다.

과목명, 분반, 강의실과 실제 교시 시간이 그리드에 표시됩니다. 평일은 기본으로 보이며 토요일이나 일요일 수업이 있는 경우 해당 요일도 추가됩니다.

## 4. PNG로 내보내기

앱은 시간표를 자동 저장하지 않습니다.

### 현재 시간표

1. 내보낼 조합을 선택합니다.
2. 상단의 **PNG 내보내기** 버튼을 누른 뒤 **현재 시간표 …번만 내보내기**를 선택하거나 `Ctrl+E`를 누릅니다.
3. 저장할 폴더를 선택합니다.

### 전체 시간표

1. 상단의 **PNG 내보내기** 버튼을 누른 뒤 **모든 시간표 …개 내보내기**를 선택하거나 `Ctrl+Shift+E`를 누릅니다.
2. 저장할 폴더를 선택합니다.
3. 진행 중 취소하려면 `Esc`를 누릅니다.

파일명은 `{CSV 이름}_시간표_{번호}.png` 형식입니다. 같은 이름의 파일이 이미 있으면 기존 파일을 덮어쓰지 않고 `(2)`, `(3)`과 같은 번호를 붙여 저장합니다.

## 5. 단축키 모음

| 단축키 | 동작 |
| --- | --- |
| `Ctrl+O` | CSV 불러오기 |
| `Ctrl+E` | 현재 시간표 PNG 내보내기 |
| `Ctrl+Shift+E` | 전체 시간표 PNG 내보내기 |
| `Alt+Left` | 이전 시간표 |
| `Alt+Right` | 다음 시간표 |
| `Esc` | 진행 중인 작업 취소 |

## 6. 문제 해결

### CSV를 불러올 수 없음

화면에 표시된 행, 열, 오류 내용을 확인합니다. 헤더 철자와 순서, UTF-8 인코딩, 각 행의 열 수, `TimeSlots` 형식을 먼저 점검하세요. 수정한 뒤 오류 화면의 **다른 CSV 선택** 또는 상단의 **CSV 불러오기**로 파일을 다시 선택할 수 있습니다. 이전에 열어 둔 시간표가 있으면 **이전 시간표로 돌아가기**도 사용할 수 있습니다.

### 유효한 시간표가 없음

각 `CourseId`에서 하나씩 선택했을 때 모든 경우에 시간 충돌이 발생한다는 뜻입니다. 서로 다른 `CourseId`의 `TimeSlots`을 확인하거나 대안 분반을 추가하세요.

### 지원하지 않는 교시가 있음

제품 시간표는 1~10교시를 지원합니다. 0교시나 11교시 이상을 1~10교시 범위로 수정하세요.

### PNG를 저장할 수 없음

선택한 폴더가 존재하는지와 쓰기 권한이 있는지 확인한 뒤 다시 내보내세요. 기존 PNG는 자동으로 덮어쓰지 않으므로 삭제할 필요가 없습니다.
