# 제품 디자인 QA

## 비교 자료

- 기준 시안: `docs/design/planning-workspace-target.png`
- 실제 렌더: `tests/TimetableGenerator.Desktop.Tests/TestResults/planning-workspace-1487x1058.png`
- 계획 닫기 상태: `tests/TimetableGenerator.Desktop.Tests/TestResults/plan-close-dialog-1487x1058.png`
- Windows 문제 화면: `tests/TimetableGenerator.Desktop.Tests/TestResults/product-design-audit-2026-07-16/01-current-windows.png`
- Windows 최종 화면: `tests/TimetableGenerator.Desktop.Tests/TestResults/product-design-audit-2026-07-16/04-live-window-final.png`
- 비교 해상도: 1487 × 1058

## 검증 결과

- 정보 구조: 과목 탐색, 추천 시간표, 내 계획의 3개 작업 영역과 시각적 우선순위가 기준 시안과 일치한다.
- 창 동작: Windows 125% 배율에서 기존 `1440 × 900 DIP` 창이 실제 작업 영역보다 커져 위쪽 `-33 px`, 아래쪽 `49 px`가 가려지던 원인을 제거했다. 최종 창은 `1920 × 1020 px` 작업 영역 안의 `(60, 30)–(1878, 999)`에 배치되고 기본 최소화·최대화·닫기 버튼이 모두 보인다.
- 드래그: 제품 헤더의 비조작 영역은 실제 Windows `WM_NCHITTEST`에서 `HTCAPTION(2)`으로 확인했고 도움말은 별도 사용자 조작 영역으로 유지했다.
- 계획 관리: 닫기 동작은 모든 탭에서 같은 위치에 보인다. 마지막 계획에서는 버튼을 숨기지 않고 비활성화하며 이유를 도구 설명과 접근성 도움말로 제공한다. 확인 대화상자는 중앙에 표시되고 배경 상호작용을 차단하며 안전한 취소 버튼으로 포커스를 이동한다.
- 탭 확장성: 계획 이름은 제한된 폭에서 말줄임표와 전체 이름 도구 설명을 제공한다. 다수 계획은 독립 스크롤되고 새 계획 버튼은 마지막 탭 바로 옆의 보이는 위치를 유지한다.
- 헤더: 높이를 줄이고 정상 저장 상태의 시각적 강조를 낮췄다. 실제 동작이 없는 설정·데이터 정보 노출은 없으며 도움말과 자동 저장 상태만 유지한다.
- 도움말: 작업 영역을 크게 덮던 설명을 간결하게 줄이고 명시적 닫기와 `F1` 접근을 제공한다. 닫은 뒤에는 도움말 버튼으로 포커스가 돌아간다.
- 빈 상태: 추천이 없을 때 `0 / 0`, 비활성 탐색, PNG 저장, 빈 시간 미정 섹션, 추천 기준을 숨긴다. 한 개의 짧은 시작 안내와 계획 상태만 남긴다.
- 시각 체계: 선택되지 않은 과목의 임의 색상 줄을 제거하고, 실제 계획에 추가된 과목만 강조한다. 텍스트용·채움용·포커스용 색 토큰을 분리해 라이트·다크 테마 대비를 독립적으로 보장한다.
- 긴 콘텐츠: 과목명, 담당교원, 시간, 강의실은 검색 결과·계획·시간표 카드에서 잘리지 않고 줄바꿈된다. 시간표 카드는 상세 정보 flyout을 제공하며 PNG에도 전체 내용이 포함된다.
- 작은 창: 최소 높이 640에서 오른쪽 패널의 시간 미정 과목과 추천 기준까지 스크롤로 접근할 수 있다.
- 키보드·접근성: 장식용 목록 컨테이너는 탭 순서에서 제외하고 실제 추가·제거·닫기 동작에는 포커스 표시와 구체적인 접근성 이름을 제공한다. `Ctrl+F`와 `Cmd+F`는 과목 패널을 열고 검색 입력으로 포커스를 이동한다.
- 상태 표현: 시간 미정 과목이 없으면 해당 섹션을 숨기고, 저장 복구·데이터 갱신·PNG 내보내기 상태가 서로 오래 남거나 덮이지 않도록 정리했다.
- 의도적 차이: 기준 시안의 설정 버튼은 사용자 요구에 따라 제거했으며, 추천 조합 버튼은 현재 제품 범위에 맞는 PNG 저장 동작으로 대체했다.

## 핵심 흐름 상태

1. 실행과 창 조작 — 해결: 작업 영역 배치, 기본 창 버튼, 드래그 영역을 Windows 실기에서 확인했다.
2. 빈 계획 시작 — 해결: 실행할 수 없는 추천 명령과 중복 설명을 제거하고 첫 과목 추가에 집중시켰다.
3. 계획 생성과 닫기 — 해결: 새 계획과 닫기 affordance의 위치·상태·설명을 일관되게 만들었다.
4. 키보드와 대비 — 해결: 주요 단축키, 포커스 복귀, 테마별 대비 임계값을 자동 테스트한다.

화면 캡처만으로 스크린리더 전체 동작이나 WCAG 준수를 확정하지는 않는다. 실제 스크린리더별 읽기 순서와 운영체제 고대비 모드는 출시 전 수동 검증 대상으로 남긴다.

## 회귀 검증

- 기본 작업 화면과 계획 닫기 상태의 헤드리스 렌더를 생성했다.
- 최소 높이 패널 스크롤, 장식용 카드 포커스 제외, 실제 버튼 포커스, 탭 넘침, 중앙 대화상자, 긴 카드 내용, PNG 전체 높이, 저장 취소 상태를 자동 테스트한다.
- 빈 계획·일반 시간표·시간 미정 과목 상태 전이, 마지막 계획 닫기 표시, F1 도움말, 검색 단축키, 색 대비를 추가로 자동 테스트한다.
- Release 솔루션 테스트 311개가 실패나 건너뜀 없이 통과했다.
- `dotnet format --verify-no-changes`, `git diff --check`, Windows x64 게시 검증을 통과했다.

final result: passed
